" lowercase = navigate
bind j tabprev
bind k tabnext
bind h back
bind l forward

" Shift = scroll
bind J scrollline 10
bind K scrollline -10
bind H scrollpx -50
bind L scrollpx 50

" Ctrl-z toggles ignore mode
bind <C-z> mode ignore
bind --mode=ignore <C-z> mode normal
